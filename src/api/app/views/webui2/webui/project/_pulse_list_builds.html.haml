- if @builds.any?
  - builds = @builds.group(:event_type).count
  There have been
  %b
    = builds.values.sum
    builds
  of which
  %b.text-danger
    = builds['build_fail']
    failed
  and
  %b.text-success
    = builds['build_success']
    succeeded
  \.
